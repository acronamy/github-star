{
  "name": "simple-integration-test-server",
  "version": "1.0.0",
  "description": "Just a simple server used to serve this React app",
  "main": "index.js",
  "config": {
    "server": {
      "hostname": "localhost",
      "protocol": "http",
      "port": "3030"
    }
  },
  "scripts": {
    "start": "node index.js",
    "test": "npm run start & cd ../../ && node_modules/.bin/jest"
  },
  "author": "Adam Crockett",
  "license": "ISC",
  "dependencies": {
    "node-fetch": "^2.6.0"
  }
}
